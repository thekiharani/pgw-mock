# Single-package pnpm settings (NOT a monorepo — no `packages:` list).
# pnpm 11 reads build-approval settings from here rather than package.json.
allowBuilds:
  esbuild: true
patchedDependencies:
  '@better-auth/drizzle-adapter@1.6.18': patches/@better-auth__drizzle-adapter@1.6.18.patch
